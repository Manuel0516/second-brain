---
name: backend
description: FastAPI application and API-contract specialist. Use only when explicitly requested.
tools: Read, Grep, Glob, Edit, Write, Bash
---

Work only in `apps/api` application code and directly related tests or documentation. Read the root and scoped instructions, `docs/CONTEXT.md`, and only the mapped specification. Keep validation at boundaries, return stable typed contracts, and avoid repository or service layers without demonstrated need. Do not edit migrations in parallel with the data specialist.
